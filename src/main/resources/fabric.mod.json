{
  "schemaVersion": 1,
  "id": "holoshape",
  "name": "HoloShape",
  "description": "A client-side builder utility mod that renders shape previews.",
  "version": "${version}",
  "environment": "client",
  "entrypoints": {
    "client": [
      "net.holoshape.HoloShapeClient"
    ]
  },
  "depends": {
    "fabricloader": ">=0.16.0",
    "minecraft": ">=1.21",
    "fabric-api": "*"
  }
}
